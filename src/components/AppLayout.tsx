import TodoApp from "./TodoApp";

function AppLayout() {
    return (
        <main className="p-1 mx-2 sm:w-[26rem] w-full">
            <TodoApp />
        </main>
    );
}

export default AppLayout;
